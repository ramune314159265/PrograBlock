import { appFeature } from '../features/app/feature';
import { commandFeature } from '../features/command/feature';
import { settingsFeature } from '../features/settings/feature';

export const features = {
	app: appFeature,
	command: commandFeature,
	settings: settingsFeature
}
